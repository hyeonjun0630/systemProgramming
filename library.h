#ifndef LIBRARY_H
#define LIBRARY_H

// 문자열 길이 계산 함수
int string_length(const char* str);

// 곱셈 함수
int multiply(int a, int b);

// 나눗셈 함수
float divide(float a, float b);

#endif // LIBRARY_H